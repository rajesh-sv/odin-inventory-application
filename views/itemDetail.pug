extends layout

block content
  h1 Name: #{item.name}

  p #[strong Category: ]
    a(href=item.category.url) #{item.category.name}
  p #[strong Description: ] #{item.description}
  p #[strong Price: ] #{item.price} VOIDS
  p #[strong In-Stock: ] #{item.number_in_stock}
  hr
  p
    a(href=item.url+'/delete') Delete Spaceship
  p
    a(href=item.url+'/update') Update Spaceship
